\documentclass{article}
\begin{document}
	The definition of $\omega = 2\alpha + 3\beta$ % inline math mode
	$X^3 + X_2 = $ % sub and sup script
	$e^{2x + 3}$ % multi sub or sup script
	
	$\sum_{k=1}^n$ % merge between sub and sup
	
	$d=\frac{5x}{y}$ % fractional
	
	In equation (\ref{omega eq}) we measure $\omega$ % refer to equation name for insert number of it
	
	\begin{equation} % equation
		\label{omega eq} % name of equation for refered
		\omega = \sum_{i=1}^{n}w_ix
	\end{equation}
\end{document}